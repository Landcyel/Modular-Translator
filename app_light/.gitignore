# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
env/

# Archives/backups
*.rar
*.zip
*.bak

# Dependencies/models/runtime (GB-scale, not under version control; distributed separately)
dependencies/

# Build artifacts and intermediate files (PyInstaller; /build/pyinstaller/ is
# intermediate; build/ packaging scripts are committed)
/build/pyinstaller/
dist/
build_assets/
*.spec

# Artifacts and temporary files
output/
temp/

# Local config (contains real API keys, not committed; template is default.example.json)
configs/models/API/default.json

# Only configs/ core/ ui/ app/ build/ (packaging scripts) and pytest.ini are tracked
# The following are local docs/test data/tool scripts/temporary content, not committed
tests/
tools/
archived/
PLANS/
summary/
idea/
*.md
reasonix.toml

# IDE
.idea/
.vscode/

# System
Thumbs.db
.DS_Store

# Local tool state and runtime logs
.reasonix/
logs/
characters/
test_data/

# Physical copies of the dual versions (app_workflow / app_lite) — point-in-time
# snapshots re-exported from git branches by tools/sync_version_dirs.py; not under
# version control (authoritative source = git branch)
app_workflow/
app_lite/
